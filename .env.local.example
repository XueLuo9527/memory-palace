# 记忆宫殿 - 环境变量配置示例

# ========== 可选配置 ==========
# 以下环境变量用于接入 Supabase 云端存储
# 如果不需要云端同步，可以留空，应用会使用本地存储模式

# Supabase 项目 URL
# 获取方式：登录 supabase.com → 进入项目 → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase 匿名密钥
# 获取方式：登录 supabase.com → 进入项目 → Settings → API → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ========== 使用说明 ==========

# 本地开发模式（无需 Supabase）：
# 1. 复制此文件为 .env.local
# 2. 保持变量为空即可
# 3. 运行 npm run dev

# 云端同步模式（需要 Supabase）：
# 1. 复制此文件为 .env.local
# 2. 填入你的 Supabase 配置
# 3. 运行 npm run dev

# Vercel 部署：
# 在 Vercel 项目设置 → Environment Variables 中添加
# 或者直接留空使用本地存储模式
